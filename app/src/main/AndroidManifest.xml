<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sunysan.wherego" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".test.TestActivity"/>
        <activity android:name=".test.recycler_text.RecyclerViewTestAct"/>

        <activity android:name=".test.recycler_text.AddHearAndFootAct"/>
        <activity android:name=".test.recycler_text.GridRecyclerViewAct"/>
        <activity android:name=".test.recycler_text.EmptyActivity"/>
        <activity android:name=".test.recycler_text.MultiTypeAct"/>
    </application>

</manifest>